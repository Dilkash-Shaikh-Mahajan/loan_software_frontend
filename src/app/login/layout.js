export const metadata = {
  title: "Secure Login | ApexLoan",
  description: "Log in to ApexLoan to access your enterprise loan management dashboard, track collections, and analyze portfolios securely.",
  robots: {
    index: true,
    follow: true,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
